# Copiar este archivo a ".env" (que esta en .gitignore, nunca se commitea)
# y definir una contraseña propia antes de correr `docker compose up`.
# SQL Server exige: minimo 8 caracteres, con mayusculas, minusculas,
# numeros y simbolos.
MSSQL_SA_PASSWORD=CambiaEsto#2026
